// STUB — ticket 011 (PRD v2 §6, Workstream E). Not implemented yet.
//
// Home for GET /state/:id and PUT /state/:id. Contract pinned by
// server/state.test.js. Mount both in app.js BEFORE the SPA catch-all.
