// /observability dashboard + /debug/* routes — PRD v2 §9 (Workstream H), H1/H3/H4.
//
// STUB (ticket 019). The contract the implementer must satisfy is pinned in
// `server/observability.test.js`. These routes MUST be registered above the SPA
// catch-all in `server/app.js`.
