# TDD Config

## Working branch
main

## Test commands
- Unit tests: `npm run test --run` (Vitest, from `client/`)
- E2E: manual Chrome verification (no Playwright in scope for MVP)
- Full suite: `npm run test --run` from `client/`

## Proxy commands
- Start proxy: `node server/index.js` (port 3001)
- Start client: `npm run dev` from `client/` (port 5173)

## Verified: 2026-07-29
